import React from "react";
import { View, Text,
Button, TouchableOpacity } from "react-native";

const Landing = () => {
    return(
        <View>
            <Text>
                Hello
            </Text>
        </View>
    );
}
export default Landing;